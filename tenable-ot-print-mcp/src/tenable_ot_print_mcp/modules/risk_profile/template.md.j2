_Vulnerability findings: {{ vuln_returned_count }} of {{ vuln_total_count }} shown &middot; Policy violation findings: {{ policy_finding_returned_count }} of {{ policy_finding_total_count }} shown &middot; Communication peers: {{ peer_returned_count }} of {{ peer_total_count }} shown._

## Asset

| Field | Value |
|---|---|
| Name | {{ asset.name }} |
| Type | {{ asset.type }} ({{ asset.super_type }}) |
| Risk | {{ "%.1f"|format(asset.total_risk) if asset.total_risk is not none else "-" }} |
| Vendor / Model | {{ asset.vendor }} / {{ asset.model }} |
| Description | {{ asset.description }} |
| Firmware | {{ asset.firmware_version }} |
| OS / Family | {{ asset.os }} / {{ asset.family }} |
| Purdue Level | {{ asset.purdue_level }} |
| Criticality | {{ asset.criticality }} |
| Run State | {{ asset.run_status }} ({{ asset.extended_run_status }}) |
| Location | {{ asset.location }} |
| IPs | {{ asset.ips | join(", ") }} |
| MACs | {{ asset.macs | join(", ") }} |
| Segments | {{ asset.segments | join(", ") }} |
| Unresolved Events | {{ asset.unresolved_events }} |
| First Seen | {{ asset.first_seen }} |
| Last Seen | {{ asset.last_seen }} |
| Lifecycle Status | {{ asset.lifecycle_status }} |

## {{ risk_model }} Risk Assessment

{% if risk_grades %}
| {% for dim in risk_grades %}{{ (dim ~ " (" ~ risk_dimension_labels[dim] ~ ")") if dim in risk_dimension_labels else dim }} | {% endfor %}
|{% for dim in risk_grades %} :---: |{% endfor %}
| {% for dim, value in risk_grades.items() %}{{ value }} | {% endfor %}

{% for line in risk_scale_note %}
> {{ line }}
{% endfor %}
{% else %}
_No risk grades supplied for this report._
{% endif %}

## Vulnerability Findings (ranked by VPR)

{% if vulnerabilities %}
| Plugin | Severity | VPR Score | VPR Level | CVSS3 | CVEs | Exploit Available | KEV |
|---|---|---:|---|---:|---|:---:|:---:|
{% for v in vulnerabilities %}| {{ v.plugin_name }} | {{ v.severity }} | {{ v.vpr_score }} | {{ v.vpr_level }} | {{ v.cvss3_score }} | {{ v.cves }} | {{ "Yes" if v.exploit_available else "No" }} | {{ "Yes" if v.kev else "No" }} |
{% endfor %}
{% else %}
_No vulnerability findings returned for this asset._
{% endif %}

## Policy Violation Findings (ranked by severity)

{% if policy_findings %}
| Severity | Status | Policy | Plugin | Src | Dst | Protocols | MITRE Techniques | First Hit | Last Hit | Active Hits | Resolved Hits |
|---|---|---|---|---|---|---|---|---|---|---:|---:|
{% for f in policy_findings %}| {{ f.severity }} | {{ f.status }} | {{ f.policy_title }} | {{ f.plugin_name }} | {{ f.src_names }} | {{ f.dst_names }} | {{ f.protocols }} | {{ f.mitre_techniques }} | {{ f.first_hit_time }} | {{ f.last_hit_time }} | {{ f.active_hits }} | {{ f.resolved_hits }} |
{% endfor %}
{% else %}
_No policy violation findings returned for this asset._
{% endif %}

## Communication Peers (1-Hop Attack-Pathway Neighborhood)

{% if peers %}
| Peer | Protocols | Industrial Protocols | Traffic | Conversations | Last Seen |
|---|---|---|---:|---:|---|
{% for p in peers %}| {{ p.peer_display }} | {{ p.protocols }} | {{ p.industrial_protocols }} | {{ p.traffic }} | {{ p.conversation_count }} | {{ p.last_conversation }} |
{% endfor %}
{% else %}
_No communication peers returned for this asset._
{% endif %}

## Analyst Assessment

{% if analyst_assessment %}
{% for line in analyst_assessment %}- {{ line }}
{% endfor %}
{% else %}
_None supplied._
{% endif %}

## Data Limitations

{% if data_limitations %}
{% for line in data_limitations %}- {{ line }}
{% endfor %}
{% else %}
_None._
{% endif %}
